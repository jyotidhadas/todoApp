{
  "name": "json-server",
  "version": "1.0.0",
  "description": "json -server for to-do app",
  "main": "json-server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
